/**
 * Data Catalog component — Phase 2 stub.
 *
 * TODO Phase 2: Browse available data products with descriptions, schemas,
 * sample records, and freshness indicators. Products the user hasn't
 * subscribed to are visible but grayed out with "Contact Sales" prompt.
 */

export default function DataCatalog() {
  return (
    <div className="rounded-xl border bg-white p-8 shadow-sm">
      <h3 className="text-lg font-semibold text-gray-900">Data Catalog</h3>
      <p className="mt-2 text-sm text-gray-500">
        Phase 2: Browsable data product catalog with subscription-aware access.
      </p>
    </div>
  );
}
